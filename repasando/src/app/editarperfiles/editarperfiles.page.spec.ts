import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarperfilesPage } from './editarperfiles.page';

describe('EditarperfilesPage', () => {
  let component: EditarperfilesPage;
  let fixture: ComponentFixture<EditarperfilesPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarperfilesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
